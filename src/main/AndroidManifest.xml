<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.um">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainView" />
        <activity android:name=".set"/>
        <activity android:name=".weather_agree"/>
        <activity android:name=".weather"/>
        <activity android:name=".parents"/>
        <activity android:name=".user_signupActivity"/>
        <activity android:name=".gpsActivity"/>
        <activity android:name=".infoActivity"/>
        <activity android:name=".info_editActivity"/>

    </application>
   <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> //양질의 위치서비스를 받아올 수 있는권한
   // <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> //낮은 질의 비용이 적게드는 위치서비스를 받아올 수 있는 권한
-->
</manifest>